---
# Galaxy-only deps for `ade install -r collections/requirements.yml` / ansible-galaxy.
# The adjacent collection `apme.deploy_aws` is not listed here; Ansible picks it up from
# `collections/ansible_collections/` with `collections_paths = .` in ansible.cfg.
collections:
  - name: cloud.terraform
    version: ">=4.0.0,<5.0.0"
