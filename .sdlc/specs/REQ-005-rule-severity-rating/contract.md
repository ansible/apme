# REQ-005: Rule Rating & Severity System — Contract

## Status

Placeholder — to be completed during implementation planning.

## Contract Notes

- Violation message in common.proto may need a `severity` field
- Severity config schema (`.apme/severity.yml`)
- REST/gRPC API for severity CRUD from UI
